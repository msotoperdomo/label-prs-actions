name: 'Label Prs'
description: 'Manage pr labels'
inputs:
  task:  # id of input
    description: 'The task you want to run'
    required: true
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.task }}